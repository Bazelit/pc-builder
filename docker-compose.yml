services:
  postgres:
    image: postgres:latest
    container_name: postgres-pc-builder
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pc_builder_db
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
